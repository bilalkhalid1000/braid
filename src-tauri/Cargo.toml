[package]
name = "braid"
version = "0.1.0-alpha.1"
description = "A fast, keyboard-first Git GUI"
authors = ["you"]
edition = "2021"

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "braid_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"

# Desktop only: there is no self-update on mobile, and the plugin does not
# build for those targets.
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["process", "sync", "time", "rt-multi-thread", "macros", "io-util", "fs"] }
notify = "6.1"
dashmap = "6"
thiserror = "1"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
strip = true
