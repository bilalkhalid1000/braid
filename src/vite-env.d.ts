/// <reference types="vite/client" />

/** Prism's grammar files register themselves on the global and export
 *  nothing typed; DefinitelyTyped covers only the core. */
declare module "prismjs/components/prism-*";
